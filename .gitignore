# Python
__pycache__/
*.pyc
*.pyo

# System
.DS_Store
*.tmp
~$*
.~lock.*

# Large files (모델 체크포인트)
checkpoints/
models/
models.zip
backup_v1/
*.pt

# outputs/ 내 바이너리/대용량 파일 제외 (CSV, JSON, PNG만 유지)
outputs/runs/**/model_*.pt
outputs/runs/**/model_bundle.pkl
outputs/runs/**/predictions.pkl
outputs/data_splits.pkl
outputs/vader_features.pkl

# data/는 포함 (12MB, 대시보드 Data Explorer에 필요)
# outputs/는 포함 (38MB, 대시보드 시각화에 필요)

# Matplotlib cache
.mplconfig/
.cache/

# Reference papers (저작권)
빅데프 참고문헌pdf/
v1/빅데프 참고문헌pdf/

# 개인정보 포함 파일 (학생 실명·학번·이메일·전화 — 추적 제외, 로컬 보존)
*수행계획서*
*.hwp
*개발자 키우기*
*최종발표_올에이프로젝트*
# 미추적 로컬 작업 폴더 (실험 결과는 v2/outputs로 이미 반영)
hatespeech_결과분석/
hatespeech_v2_15seed_essential_*/
# 루트 발표/문서 작업물 (실험 데이터 아님 — 발표자료·node_modules·팀 Q&A)
/outputs/
node_modules/

# v1 legacy generated / large artifacts
v1/checkpoints/
v1/models/
v1/models.zip
v1/outputs/runs/**/model_*.pt
v1/outputs/runs/**/model_bundle.pkl
v1/outputs/runs/**/predictions.pkl
v1/outputs/data_splits.pkl
v1/outputs/vader_features.pkl

# IDE
.vscode/
.idea/

# Claude Code
.claude/
