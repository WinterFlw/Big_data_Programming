# v2 Scripts

보조 스크립트를 두는 자리다.

현재 주요 실행은 아래에서 처리한다.

```bash
./run.sh e2e ...
```

추가할 수 있는 스크립트:

```text
server_preflight.sh
collect_artifacts.sh
retry_failed_runs.sh
```

현재 포함된 스크립트:

```text
validate_commit_message.py
install_commit_msg_hook.sh
```
